{% extends 'base.html.twig' %}

{% block title %} Liste des publications - {{ parent() }}{% endblock %}

{% block body %}

    <ul>
        {% for post in posts %}
        <li>{{ post.title }}</li>
        {% endfor %}
    </ul>

{% endblock %}
